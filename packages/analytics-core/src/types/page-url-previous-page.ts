export interface PageUrlPreviousPageOptions {
  /**
   * Suppresses tracking previous page and page url properties except for Page Viewed and Element Clicked.
   * @defaultValue `false`
   */
  restrictToAutocapture?: boolean;
}
