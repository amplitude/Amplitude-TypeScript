import { PACKAGE_NAME } from '../src';

describe('@amplitude/utils-element-selector — scaffolding smoke test', () => {
  it('exports the package name', () => {
    expect(PACKAGE_NAME).toBe('@amplitude/utils-element-selector');
  });
});
